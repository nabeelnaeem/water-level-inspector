# Optional. Override the backend base URL. If unset, the dashboard talks to
# the same hostname it was loaded from, on port 4000 (typical LAN setup where
# the dashboard and backend run on the same device).
# VITE_API_BASE=http://192.168.1.50:4000
